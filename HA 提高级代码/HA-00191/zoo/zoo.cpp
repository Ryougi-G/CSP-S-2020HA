#include <bits/stdc++.h>
using namespace std;
int main(){
	freopen("zoo.in", "r", stdin);
	freopen("zoo.out", "w", stdout);
	int n, m, c, k;
	cin >> n >> m >> c >> k;
	int sum = 0;
	int a[n];
	for(int i = 0; i < n; i++){
		cin >> a[i];
	}
	int s1[m], s2[m];
	for(int i = 0; i < m; i++){
		cin >> s1[i] >> s2[i];
	}
	return 0;
}
