#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("call.in", "r", stdin);
    freopen("call.out", "w", stdout);
    int n;
    cin >> n;
    int a[n];
    for(int i = 0; i < n; i++){
    	cin >> a[i];
    }
    int m;
    cin >> m;
    
	return 0;
}
