#include <iostream>
#include <stream>
using namespace std;
int main(){
	freopen("call.in", "r", stdin);
	freopen("call.out", "w", stdout);
	int n;
	cin >> n;
	int a[i];
	cin >> a[i];
	for (int i = 0; i < n; i++){
		i = a[i].size();
	}
	int m;
	cin >> m;
	for (int j = 0; j < m; j++){
		
	}
	return 0;
}
