#include <iostream>
#include <stream>
using namespace;
int main(){
	freopen("zoo.in", "r", stdin);
	freoprn("zoo.out", "w", stdout);
	int n, m, c, k;
	cin >> n >> m >> c >> k;
	int a;
	int p, q;
	for (int i = 0; i < n; i++){
		cout << a[i];
	}
	for (int j = 0; j < m; j++){
		cout << p[i] << q[i];
	}
	
	return 0;
}
