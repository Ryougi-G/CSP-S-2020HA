#include<iostream>
#include<cstdio>
using namespace std;
int main(){
	freopen("snakes.in","r",stdin);
	freopen("snakes.out","w",stdout);
	int n;
	cin>>n;
	if (n<=0){
		fclose(stdin);
	fclose(stdout);
		return 0;
	}
	
	fclose(stdin);
	fclose(stdout);
	return 0;
}
