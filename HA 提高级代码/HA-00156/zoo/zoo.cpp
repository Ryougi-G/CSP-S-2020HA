#include<bits/stdc++.h>
using namespace std;
long long n,m,c,k;
int main()
}
	freopen("zoo.in","r",stdin);
	freopen("zoo.out","w",stdout);
	cin>>n>>m>c>>k;
	int a[n],b[m],c[m];
	for(int i=0;i<n;i++)
	cihn>>a[i];
	for(int i=0;i<n;i++)
	cin>>b[i]>>c[i];
	cout<<"2"<<endl;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
